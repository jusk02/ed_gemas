<%= form_for(@techers_assignment) do |f| %>
  <% if @techers_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@techers_assignment.errors.count, "error") %> prohibited this techers_assignment from being saved:</h2>

      <ul>
      <% @techers_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label :teacher_id %><br>
    <%= f.select :teacher_id, Teacher.all.collect{|u|[u.name + " " + u.lastname, u.id]} %><br>
    
  </div>
  <div class="field">
    <%= f.label :assignment_id %><br>
    <%= f.select :assignment_id, Assignment.all.collect{|u|[u.name, u.id]} %><br>
  </div>
  <div class="actions">
    <%= f.submit "tomar materia", :url => root_path %>
  </div>
<% end %>
